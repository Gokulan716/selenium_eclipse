package Practice_Questions;

/* 
Create an interface Shape with a method draw(), and another interface Colorable with a method color(). 
Create a class Circle that implements both interfaces. Implement the methods draw() and color() in Circle. In the main method, 
create an instance of Circle and call both methods. 
*/

public interface QA19_Shape {

	 void  draw();
}
