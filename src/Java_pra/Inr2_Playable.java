package Java_pra;


/*
 Write a Java program that defines an interface Playable with a method play(). 
 Then, create an abstract class Media with an abstract method getTitle(). Implement a class Song that extends Media and implements Playable. 
 The class should have fields for the title, artist, and duration of the song. Provide implementations for all the methods in the class. 
 In the main method, create an instance of Song and print the title, artist, and duration, and then call the play() method. 
 */
 interface Inr2_Playable {

	 void play();
}
